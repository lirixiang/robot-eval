# Secrets — create before applying other manifests
# Usage: kubectl apply -f 04-secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: robot-eval-secrets
  namespace: robot-eval
type: Opaque
stringData:
  db-password: "eval_secret"   # CHANGE THIS in production
